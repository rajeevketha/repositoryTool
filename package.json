{
  "name": "orgflow",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension for Jira-versioned Salesforce org-to-org deployments stored in Git.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
